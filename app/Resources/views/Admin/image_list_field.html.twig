{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    {% spaceless %}
        <div>
            {% if object.imageName %}
                {#<img src="{{ vich_uploader_asset(object, 'logoFile') | imagine_filter('60x60') }}" alt="">#}
                +++
            {% else %}
                ---
            {% endif %}
        </div>
    {% endspaceless %}
{% endblock %}
